// Basic maths operations bhi hai i.e =,-,/,% etc.

// String1 + String2 se concatenate ho jayegi strings i.e jud jayengi.

console.log("1" + 2) // Gives 12 i.e concatenated
console.log(1 + "2") // Gives 12
console.log("1" + 2 + 2) // Gives 122 na ki 14 (2+2=4)
console.log(1 + 2 + "2") // Gives 32 bncho, na ki 122

// NOTE - Agar String pehle hai to string mea sb convert else if kuch or oehle hai to pehle vo operation, then string mea convert.

console.log(true) // Output = true
console.log(+true) // Output = 1 (converted)
console.log(+"") // Output = 0 (converted)

// ++ and -- bhi kaam karte hai i.e number ko increment karne mea.

// y = x++ kiya to y = x old value since pehle save kiya then INCREMENT KIYA i.e x will be incremented as x =  x + 1

// y = ++x kiya to y = x + 1 since pehle increment kiya then save kiya i.e x will be incremented too as x =  x + 1

// a ** b means a ki power b i.e base ** exponent